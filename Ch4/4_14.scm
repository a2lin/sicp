;Louis's map fails even though Eva's works because the normal 'map' 
;does not evaluate the primitives like a defined 'map' will.
;Mixing the two evaluation strategies will result in problems
;as the real evaluator and the interpreted evaluator do not
;communicate this state to each other.
