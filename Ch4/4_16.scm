; 
;(define (f x)
;  (define (even? n) 
;    (if (= n 0) 
;      true 
;      (odd? (- n 1)))) 
;  (define (odd? n) 
;    (if (= n 0) 
;      false 
;      (even? (- n 1)))) 
;  ⟨rest of body of f⟩)
;
; Design mechanism to make this valid:: both need
; to be added at the same time

