; environment diagram ->

; memoize
; let -> table
; -> lambda
;    -> compute result and shove in table

; continue to use the let
; calculate results.


; memo-fib stores the last computed element so it does not need to recomput the elements.
; this would most certainly not work, because different tables would be used.

