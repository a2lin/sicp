; this procedure reverses the list

; winds up with something like [d][=>][c][=>][b][=>][a][=>][][\]
;                                                    ^--v

; I would expect v -> list('a), and w -> list('d 'c 'b 'a)
